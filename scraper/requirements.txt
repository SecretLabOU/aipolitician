# Core dependencies
requests>=2.28.0
python-dotenv>=0.20.0
sentence-transformers>=2.2.2
pymilvus>=2.2.0

# For UUID generation (standard library uuid is used in the code)
# No external package needed for uuid

# Data processing
json5>=0.9.6

# Type hints
typing-extensions>=4.3.0

# Note: We're using the mock crawler by default since Crawl4AI Docker image
# is only available for ARM64 architecture. If you're on an ARM64 system,
# you can run the Docker container with:
# docker run -p 11235:11235 unclecode/crawl4ai:basic 