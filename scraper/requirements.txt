# Core dependencies
requests>=2.28.0
python-dotenv>=0.20.0
sentence-transformers>=2.2.2
pymilvus>=2.2.0

# For UUID generation (standard library uuid is used in the code)
# No external package needed for uuid

# Crawl4AI dependencies (optional, uncomment if using Crawl4AI directly)
crawl4ai>=0.1.0

# Data processing
json5>=0.9.6

# Type hints
typing-extensions>=4.3.0 