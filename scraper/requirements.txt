# Core dependencies
requests>=2.28.0
python-dotenv>=0.20.0
sentence-transformers>=2.2.2
pymilvus>=2.2.0

# For UUID generation (standard library uuid is used in the code)
# No external package needed for uuid

# Data processing
json5>=0.9.6

# Type hints
typing-extensions>=4.3.0

# Note: We're using the Crawl4AI API via Docker, not as a Python package
# Run the Docker container with:
# docker run --platform linux/amd64 -p 11235:11235 unclecode/crawl4ai:latest 